{
  "name": "smart-order-fulfillment",
  "version": "1.0.0",
  "description": "Serverless idempotent checkout service for e-commerce",
  "main": "dist/handlers/checkout.js",
  "scripts": {
    "build": "tsc",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "tsc --noEmit",
    "cdk": "cdk",
    "dev": "npm run build && npx ts-node scripts/local-server.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0",
    "uuid": "^9.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.0",
    "@types/uuid": "^9.0.7",
    "aws-cdk": "^2.120.0",
    "aws-cdk-lib": "^2.120.0",
    "aws-sdk-client-mock": "^3.0.1",
    "constructs": "^10.3.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
